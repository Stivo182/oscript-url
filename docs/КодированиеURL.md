# КодированиеURL

Публичный модуль кодирует компоненты URI и параметры формы. Функции не изменяют входные строки и не выполняют
сетевых операций.

## Оглавление

- [Подключение](#подключение)
- [Выбор функции](#выбор-функции)
- [Функции](#функции)
  - [ДекодироватьВБайты](#декодироватьвбайты)
  - [ДекодироватьПараметрФормы](#декодироватьпараметрформы)
  - [ДекодироватьUTF8](#декодироватьutf8)
  - [КодироватьДанныеПользователя](#кодироватьданныепользователя)
  - [КодироватьСтрокуЗапроса](#кодироватьстрокузапроса)
  - [КодироватьИмяПользователя](#кодироватьимяпользователя)
  - [КодироватьПараметрФормы](#кодироватьпараметрформы)
  - [КодироватьПароль](#кодироватьпароль)
  - [КодироватьПуть](#кодироватьпуть)
  - [КодироватьСегментПути](#кодироватьсегментпути)
  - [КодироватьФрагмент](#кодироватьфрагмент)
- [Связанные сущности](#связанные-сущности)

## Подключение

[`КодированиеURL`](КодированиеURL.md) - общий модуль, поэтому его не создают оператором `Новый`:

```bsl
#Использовать url

Путь = КодированиеURL.КодироватьПуть("/Иван Петров");
```

## Выбор функции

Передавайте функциям декодированные данные. Буквальный `%` считается обычным символом и кодируется как `%25`,
поэтому уже записанное `%2F` превратится в `%252F`. Чтобы сохранить готовое процентное кодирование компонента,
используйте соответствующий метод `УстановитьКодированный...()` класса [`URL`](URL.md).

| Задача | Функция | `/` | `&` и `=` | `+` | Пробел |
| --- | --- | --- | --- | --- | --- |
| Закодировать полный путь с разделителями | `КодироватьПуть()` | Сохраняется | Сохраняются | Сохраняется | `%20` |
| Закодировать одно значение сегмента | `КодироватьСегментПути()` | `%2F` | Сохраняются | Сохраняется | `%20` |
| Запрос как единый компонент URI | `КодироватьСтрокуЗапроса()` | Сохраняется | Сохраняются | Сохраняется | `%20` |
| Закодировать имя или значение параметра формы | `КодироватьПараметрФормы()` | `%2F` | `%26` и `%3D` | `%2B` | `+` |

Пример для строки `a/b?x=1&y=2+3%20`:

```text
КодироватьПуть              a/b%3Fx=1&y=2+3%2520
КодироватьСегментПути       a%2Fb%3Fx=1&y=2+3%2520
КодироватьСтрокуЗапроса     a/b?x=1&y=2+3%2520
КодироватьПараметрФормы     a%2Fb%3Fx%3D1%26y%3D2%2B3%2520
```

`КодироватьСтрокуЗапроса()` не разбирает пары параметров и не защищает `&` и `=` как данные. Для построения
параметров используйте [`ПараметрыЗапросаURL`](ПараметрыЗапросаURL.md). Для совместимости с веб-формой можно
явно выбрать [`ВебФорма()`](ФорматыПараметровЗапросаURL.md#вебформа) или кодировать каждое имя и значение через
`КодироватьПараметрФормы()`.

## Функции

### ДекодироватьВБайты

Строго декодирует процентные триплеты ASCII-строки в последовательность байтов.

```bsl
КодированиеURL.ДекодироватьВБайты(<Значение>)
```

#### Параметры

| Параметр | Тип | Описание |
| --- | --- | --- |
| Значение | `Строка` | ASCII-строка с процентным кодированием |

#### Возвращаемое значение

Новый объект `ДвоичныеДанные` с декодированными байтами.

#### Ошибки

| Код | Условие |
| --- | --- |
| `argument.invalid_type` | Передан неподдерживаемый тип |
| `argument.invalid_value` | Строка содержит символы вне ASCII |
| `url.invalid_percent_encoding` | Нарушена форма `%HH` |

### ДекодироватьПараметрФормы

Нестрого декодирует значение `application/x-www-form-urlencoded`: `+` становится пробелом, некорректный `%`
сохраняется, а некорректные последовательности UTF-8 заменяются на `U+FFFD`.

```bsl
КодированиеURL.ДекодироватьПараметрФормы(<Значение>)
```

#### Параметры

| Параметр | Тип | Описание |
| --- | --- | --- |
| Значение | `Строка` | Декодируемое значение формы |

#### Возвращаемое значение

`Строка` с декодированным значением.

#### Ошибки

| Код | Условие |
| --- | --- |
| `argument.invalid_type` | Передан неподдерживаемый тип |

### ДекодироватьUTF8

Строго декодирует процентные триплеты ASCII-строки, а затем декодирует полученные байты как UTF-8.

```bsl
КодированиеURL.ДекодироватьUTF8(<Значение>)
```

#### Параметры

| Параметр | Тип | Описание |
| --- | --- | --- |
| Значение | `Строка` | ASCII-строка с процентным кодированием |

#### Возвращаемое значение

`Строка`, декодированная из UTF-8.

#### Ошибки

| Код | Условие |
| --- | --- |
| `argument.invalid_type` | Передан неподдерживаемый тип |
| `argument.invalid_value` | Строка содержит символы вне ASCII |
| `url.invalid_percent_encoding` | Нарушена форма `%HH` |
| `encoding.invalid` | Байты не образуют корректную последовательность UTF-8 |

### КодироватьДанныеПользователя

Кодирует полные данные пользователя по
[RFC 3986 § 3.2.1](https://www.rfc-editor.org/rfc/rfc3986.html#section-3.2.1). Буквальный `:` сохраняется как
разделитель имени и пароля.

```bsl
КодированиеURL.КодироватьДанныеПользователя(<Значение>)
```

#### Параметры

| Параметр | Тип | Описание |
| --- | --- | --- |
| Значение | `Строка` | Кодируемые данные пользователя |

#### Возвращаемое значение

`Строка` с процентным кодированием байтов UTF-8.

#### Ошибки

| Код | Условие |
| --- | --- |
| `argument.invalid_type` | Передан неподдерживаемый тип |

### КодироватьСтрокуЗапроса

Кодирует строку запроса по
[RFC 3986 § 3.4](https://www.rfc-editor.org/rfc/rfc3986.html#section-3.4).

```bsl
КодированиеURL.КодироватьСтрокуЗапроса(<Значение>)
```

#### Параметры

| Параметр | Тип | Описание |
| --- | --- | --- |
| Значение | `Строка` | Кодируемая строка запроса без начального `?` |

#### Возвращаемое значение

`Строка` с процентным кодированием байтов UTF-8.

#### Ошибки

| Код | Условие |
| --- | --- |
| `argument.invalid_type` | Передан неподдерживаемый тип |

### КодироватьИмяПользователя

Кодирует имя пользователя. Буквальный `:` кодируется как `%3A`.

```bsl
КодированиеURL.КодироватьИмяПользователя(<Значение>)
```

#### Параметры

| Параметр | Тип | Описание |
| --- | --- | --- |
| Значение | `Строка` | Кодируемое имя пользователя |

#### Возвращаемое значение

`Строка` с процентным кодированием байтов UTF-8.

#### Ошибки

| Код | Условие |
| --- | --- |
| `argument.invalid_type` | Передан неподдерживаемый тип |

### КодироватьПараметрФормы

Кодирует значение по алгоритму `application/x-www-form-urlencoded`: пробел становится `+`, остальные символы
преобразуются в байты UTF-8 и при необходимости кодируются как `%HH`.

```bsl
КодированиеURL.КодироватьПараметрФормы(<Значение>)
```

#### Параметры

| Параметр | Тип | Описание |
| --- | --- | --- |
| Значение | `Строка` | Кодируемое значение формы |

#### Возвращаемое значение

`Строка` в представлении `application/x-www-form-urlencoded`.

#### Ошибки

| Код | Условие |
| --- | --- |
| `argument.invalid_type` | Передан неподдерживаемый тип |

### КодироватьПароль

Кодирует пароль. Буквальный `:` внутри пароля сохраняется.

```bsl
КодированиеURL.КодироватьПароль(<Значение>)
```

#### Параметры

| Параметр | Тип | Описание |
| --- | --- | --- |
| Значение | `Строка` | Кодируемый пароль |

#### Возвращаемое значение

`Строка` с процентным кодированием байтов UTF-8.

#### Ошибки

| Код | Условие |
| --- | --- |
| `argument.invalid_type` | Передан неподдерживаемый тип |

### КодироватьПуть

Кодирует строку как путь по RFC 3986 § 3.3, сохраняя структурные разделители `/`.

```bsl
КодированиеURL.КодироватьПуть(<Значение>)
```

#### Параметры

| Параметр | Тип | Описание |
| --- | --- | --- |
| Значение | `Строка` | Кодируемый путь |

#### Возвращаемое значение

`Строка` с процентным кодированием байтов UTF-8.

#### Ошибки

| Код | Условие |
| --- | --- |
| `argument.invalid_type` | Передан неподдерживаемый тип |

### КодироватьСегментПути

Кодирует строку как один сегмент пути по
[RFC 3986 § 3.3](https://www.rfc-editor.org/rfc/rfc3986.html#section-3.3). Символ `/` кодируется как `%2F`.

```bsl
КодированиеURL.КодироватьСегментПути(<Значение>)
```

#### Параметры

| Параметр | Тип | Описание |
| --- | --- | --- |
| Значение | `Строка` | Кодируемый сегмент |

#### Возвращаемое значение

`Строка` с процентным кодированием байтов UTF-8.

#### Ошибки

| Код | Условие |
| --- | --- |
| `argument.invalid_type` | Передан неподдерживаемый тип |

### КодироватьФрагмент

Кодирует строку как фрагмент по
[RFC 3986 § 3.5](https://www.rfc-editor.org/rfc/rfc3986.html#section-3.5).

```bsl
КодированиеURL.КодироватьФрагмент(<Значение>)
```

#### Параметры

| Параметр | Тип | Описание |
| --- | --- | --- |
| Значение | `Строка` | Кодируемый фрагмент без начального `#` |

#### Возвращаемое значение

`Строка` с процентным кодированием байтов UTF-8.

#### Ошибки

| Код | Условие |
| --- | --- |
| `argument.invalid_type` | Передан неподдерживаемый тип |

## Связанные сущности

- [URL](URL.md)
- [ПараметрыЗапросаURL](ПараметрыЗапросаURL.md)
